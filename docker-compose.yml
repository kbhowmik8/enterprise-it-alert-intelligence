services:
  dashboard:
    build: .
    ports: ["8501:8501"]
    restart: unless-stopped
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports: ["8000:8000"]
    restart: unless-stopped
 